# Credenziali

Usare `.secrets.example` solo come elenco. I valori reali vanno in Nhost Secrets. Non stampare token, cookie, private key o payload cifrati. In caso di sospetta esposizione: revocare, ruotare master/DEK secondo procedura, controllare audit e invalidare sessioni.
